{
  "name": "prettier-plugin-single-attribute-per-line-except-first",
  "version": "1.0.1",
  "description": "Prettier plugin that puts each attribute on its own line, except for the first attribute",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "prettier",
    "prettier-plugin",
    "formatter",
    "html",
    "jsx",
    "attributes"
  ],
  "author": "",
  "license": "MIT",
  "peerDependencies": {
    "prettier": "^2.0.0 || ^3.0.0"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "prettier": "^3.0.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.2.3"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
